# Deployment status

The Python backend is prepared for Railway/container deployment.

Architecture: Google Form -> Google Sheets -> Python FastAPI -> SQLite cache -> dashboard.

The dashboard frontend remains in `public/` and the existing UI is preserved.
